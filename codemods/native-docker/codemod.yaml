schema_version: "1.0"

name: "@runtipi/native-docker"
version: "0.1.3"
description: "Convert Runtipi docker-compose.json apps to docker-compose.yml with x-runtipi metadata"
author: "Runtipi App Store Maintainers <contact@runtipi.io>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["json", "yaml"]

keywords: ["migration", "runtipi", "docker-compose", "yaml"]

registry:
  access: "public"
  visibility: "public"
