version: '2'
services:
  quakejs:
    container_name: quakejs
    environment:
      - HTTP_PORT=8080
    ports:
      - 8080:80
      - 27960:27960
    image: treyyoder/quakejs:latest
    restart: unless-stopped
    networks:
      - tipi_main_network
