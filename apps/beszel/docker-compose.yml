services:
  beszel:
    image: henrygd/beszel:0.19.0
    volumes:
      - ${APP_DATA_DIR}/data:/beszel_data
    x-runtipi:
      internal_port: 8090
      is_main: true
x-runtipi:
  schema_version: 2
