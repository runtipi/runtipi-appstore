{
  "services": [
    {
      "name": "lobe-chat",
      "image": "lobehub/lobe-chat:1.143.2",
      "isMain": true,
      "internalPort": 3210,
      "environment": [
        {
          "key": "OPENAI_API_KEY",
          "value": "${OPENAI_API_KEY}"
        },
        {
          "key": "OPENAI_PROXY_URL",
          "value": "${OPEANAI_PROXY_URL}"
        },
        {
          "key": "CUSTOM_MODELS",
          "value": "${CUSTOM_MODELS}"
        },
        {
          "key": "ACCESS_CODE",
          "value": "${ACCESS_CODE}"
        }
      ]
    }
  ],
  "schemaVersion": 2,
  "$schema": "https://schemas.runtipi.io/v2/dynamic-compose.json"
}
