{
  "services": [
    {
      "name": "lobe-chat",
      "image": "lobehub/lobe-chat:1.81.7",
      "isMain": true,
      "internalPort": 3210,
      "environment": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "OPENAI_PROXY_URL": "${OPEANAI_PROXY_URL}",
        "CUSTOM_MODELS": "${CUSTOM_MODELS}",
        "ACCESS_CODE": "${ACCESS_CODE}"
      }
    }
  ],
  "$schema": "../dynamic-compose-schema.json"
}
